import { pageMetadata } from '@/lib/site-seo';
export const metadata = pageMetadata("Discuss Your Project", "Tell InfronixWeb about your website, marketing or automation project. Share your requirements and request a scoped proposal.", '/start-project');

export default function StartProjectLayout({ children }) {
  return children;
}
