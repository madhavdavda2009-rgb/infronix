import { pageMetadata } from '@/lib/site-seo';
export const metadata = pageMetadata("Terms & Conditions", "Read the terms that apply to the InfronixWeb website and services.", '/terms-and-conditions');

export default function TermsAndConditionsLayout({ children }) {
  return children;
}
