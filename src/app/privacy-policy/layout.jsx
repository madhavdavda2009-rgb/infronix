import { pageMetadata } from '@/lib/site-seo';
export const metadata = pageMetadata("Privacy Policy", "How InfronixWeb handles personal information and website enquiries.", '/privacy-policy');

export default function PrivacyPolicyLayout({ children }) {
  return children;
}
